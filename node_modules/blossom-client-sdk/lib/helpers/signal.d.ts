export declare class TimeoutError extends Error {
}
/** Creates and optionally wraps an AbortSignal with a timeout */
export declare function wrapSignalWithTimeout(timeout: number, message: string, signal: AbortSignal | undefined): {
    cancel: () => void;
    signal: AbortSignal;
};
